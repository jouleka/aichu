SECRET_TEXT=sk-proj-fake-1234567890abcdefghijklmnopqrstuvwxyz
SECRET_TYPE=OPENAI_KEY

Review this Python — I think the API key shouldn't be inline:

from openai import OpenAI
client = OpenAI(api_key="sk-proj-fake-1234567890abcdefghijklmnopqrstuvwxyz")
response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "hello"}],
)
print(response.choices[0].message.content)

How should I rework this to read from env and what's the standard 12-factor pattern?
