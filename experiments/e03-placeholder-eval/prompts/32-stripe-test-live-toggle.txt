SECRET_TEXT=sk_test_fake_StripePlaygroundKey
SECRET_TYPE=STRIPE_KEY

Help me set up a proper test/live mode toggle. Currently:

if os.getenv("ENV") == "production":
    stripe.api_key = "sk_live_fake_StripeTestKeyForDocs"
else:
    stripe.api_key = "sk_test_fake_StripePlaygroundKey"

This feels brittle. What's idiomatic in a 12-factor Flask app with multiple environments (local, staging, prod)?
